/**
 * @deprecated Background thread intervals are deprecated in favor of Serverless Vercel Crons.
 * This file is preserved as an empty stub to prevent legacy compiler path breakages.
 */
export function startScheduler(): void {
  // Deprecated
  return;
}
